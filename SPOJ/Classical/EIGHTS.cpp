#include <bits/stdc++.h>
using namespace std;

int main() {

    int t;
    cin >> t;

    while(t--) {
        unsigned long long int k, ans;
        cin >> k;
        cout << 192 + (k - 1) * 250 << "\n";
    }

    return 0;
}