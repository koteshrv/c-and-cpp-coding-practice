#include <bits/stdc++.h>
using namespace std;

int main() {

    int t = 1;
    cin >> t;

    while(t--) {
        int l, r;
        cin >> l >> r;
        cout << 1000003 << endl; // max r values is 1000000 so co prime for range (l, r) is 1000003 which is < 2*1e9
    }

    return 0;
}